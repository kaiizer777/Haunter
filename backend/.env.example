# Database Connection (Neon Postgres)
DATABASE_URL=postgresql://neondb_owner:npg_THmVZN79xgyQ@ep-square-sun-azk3knbd-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DATABASE_URL_UNPOOLED=postgresql://neondb_owner:npg_THmVZN79xgyQ@ep-square-sun-azk3knbd.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# GitHub OAuth App (login only — read:user scope; separate from GitHub App for repo/webhooks)
GITHUB_CLIENT_ID=Ov23liChZ8BxIKmMuQyD
GITHUB_CLIENT_SECRET=f18073dbef0a03de71e08d94e5f4b88cff342733

# Hardcoded redirect_uri — must match exactly what's registered in the GitHub OAuth App.
# Never derived from request Host header. Update this when deploying to prod.
CALLBACK_URL=http://localhost:8000/auth/callback

# Session cookie signing key (itsdangerous) — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET_KEY=90fa1fbed9177182d6d4c675784e9790aad3a78f965422745017560546270af8

# Previous signing key for zero-downtime rotation — leave empty unless rotating.
# SESSION_SECRET_KEY_PREVIOUS=

# Fernet key for encrypting users.access_token at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: not set = plaintext storage — pre-prod security blocker.
# TOKEN_ENCRYPTION_KEY=

# Frontend origin for OAuth redirect + CORS
FRONTEND_URL=http://localhost:3000

# Per-IP rate limit for auth endpoints (req/min). Default: 20.
# RATE_LIMIT_PER_MINUTE=20
